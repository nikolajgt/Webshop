namespace Webshop.Models.Base
{ 
    public enum Roles
    {
        Customer,
        Admin
    }

    public enum ProductCategory
    {
        Keyboard,
        Smartphone,
        Mircophone
    }
}
